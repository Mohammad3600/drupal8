{#
/**
 * @file
 * Theme override to display a single page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template in this directory.
 *
 * Available variables:
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration pages.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - messages: Status and error messages. Should be displayed prominently.
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Regions:
 * - page.header: Items for the header region.
 * - page.primary_menu: Items for the primary menu region.
 * - page.secondary_menu: Items for the secondary menu region.
 * - page.highlighted: Items for the highlighted content region.
 * - page.help: Dynamic help text, mostly for admin pages.
 * - page.content: The main content of the current page.
 * - page.sidebar_first: Items for the first sidebar.
 * - page.sidebar_second: Items for the second sidebar.
 * - page.footer: Items for the footer region.
 * - page.breadcrumb: Items for the breadcrumb region.
 *
 * @see template_preprocess_page()
 * @see html.html.twig
 */
#}
<style>
  #concept-table #block-sustainable-minds-page-title {
    display:none;
  }
  #concept-table table.project-concepts {
    border-collapse: collapse;
}
#concept-table .project-concepts tr {
    background-color: #F1F1F1;
}
#concept-table .project-concepts tr.even, .even tr {
    background-color: #CBE1EA !important;
}
#concept-table .project-concepts tr {
    background-color: #F1F1F1;
}
#concept-table tr.even, tr.odd {
    background-color: #eee;
    border-bottom: 1px solid #ccc;
    padding: 0.1em 0.6em;
}
#concept-table .project-concepts td {
    border: 1px solid #FFF;
    padding: 0;
    margin: 0;
    text-align: center;
}
#concept-table .project-concepts th {
    width: 90px;
    border: 1px solid #fff;
    background-color: #E2EBEE;
    color: #000;
    font-weight: normal;
    padding: 5px 5px;
    vertical-align: top;
}
#concept-table table.project-concepts table td {
    border: 0px;
    text-align: left;
}
#concept-table .project-concepts .act a {
    cursor: pointer;
    font-size:11px;
}
#concept-table table.project-concepts td.title {
    height: 10px;
}
#concept-table .project-concepts td.name {
    height: 50px;
    width: 100%;
    vertical-align: top;
    font-weight: bold;
    padding: 0 7px 0 7px;
}
#concept-table .project-concepts tr.positive, .positive tr {
    background-color: #FFE2E2 !important;
}
.project-concepts .product-view-concept-icon img, .product-view .product-view-concept-icon img {
    max-width: 100px;
    max-height: 58px;
}
.project-concepts h3 {
    font-size: 11px;
    padding-left: 5px;
    margin: 0;
    height: 30px;
    line-height: 30px;
}
.project-concepts .product-view-concept-icon div, .product-view .product-view-concept-icon div {
    width: 100px;
    background-color: #E0EAEC;
    border: 1px solid #ccc;
    text-align: center;
    height: 58px;
    line-height: 58px;
    margin: 0px 0px 5px 5px;
}
#concept-table table.project-concepts table {
    padding: 0;
    margin: 0;
    width: 100%;
    position: relative;
}
#concept-table .project-concepts table, .project-concepts td table {
    position: relative !important;
    width: 100% !important;
}
#concept-table .project-concepts .gl-more-emphesis {
    font-size: 16px !important;
}
#concept-table .gl-more-emphesis {
    font-size: 15px;
    font-weight: bold;
}
 .clear {
    clear: both;
}
.clear {
    margin: 0px;
    padding: 0px;
    clear: both;
}
#concept-table .project-concepts td.negative {
    color: #336600;
    font-weight: bold;
}
#concept-table .project-concepts .product-view-concept-icon, .product-view .product-view-concept-icon {
    width: 107px;
}
#concept-table .project-concepts th.concept-table-funcunit {
    background-color: #fff;
    padding: 3px 5px;
    width: 300px !important;
}
#concept-table .project-concepts td.name a {
    /* font-size: 13px; */
    font-size: 12px;
    font-weight: bold;
    text-decoration: none;
}
.font-size-6{
  font-size: 0.65rem;
}
  .top-9{
    top: 9.5rem;
  }
  .prevmethodology {
    background-color: #FDFFB9;
    font-size: 11px;
    margin-top: -3px;
    margin-left: -7px;
    padding: 5px 7px;
}
</style>
<div id="wrapper">
<div id="body">

    {{ page.header }}

  {{ page.primary_menu }}
  {{ page.secondary_menu }}

  {{ page.breadcrumb }}

  {{ page.highlighted }}

  {{ page.help }}

  <main>
    <div class="container">
        <div class="light_blue_grd" id="blue_grid"></div>
    </div>
    <section>
      <div class="container bg_white">
          <div class="row">
          {% set site_url = path("<current>") ~ '' %}
          {% set col_class = ('project/concept/view' in site_url or 'project/view' in site_url or 'project/concepts' in site_url) ? 'col-lg-12' : 'col-lg-9' %}
            <div class="layout-content {{ col_class }}">
              {{ page.content }}
            </div>{# /.layout-content #}

            {% if page.side_bar %}
              <div class="layout-sidebar-first col-lg-3 my-3" role="complementary">
                {{ page.side_bar }}
              </div>
            {% endif %}
          </div>
      </div>  
    </section>    

    {% if page.sidebar_second %}
      <aside class="layout-sidebar-second" role="complementary">
        {{ page.sidebar_second }}
      </aside>
    {% endif %}

  </main>

  {% if page.footer %}
    <footer role="contentinfo">
      {{ page.footer }}
    </footer>
  {% endif %}

</div>{# /.layout-container #}
</div>
